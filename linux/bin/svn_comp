#!/bin/sh

if [ -z "$1" ]
then
   echo "need argument"
   exit 1
fi

#find -name '*\.mk' | sed 's/\.mk$//' | sed 's/.*\///g' | grep "$1"
find mk -name '*mk' -exec grep '^build-' \{} \; | sed 's/^build\-\([^:]*\):.*$/\1/' | grep "$1"

